# admob_mediation_flutter example

This runnable Android/iOS app demonstrates every supported ad format using
Google's public sample App IDs and test ad unit IDs.

```sh
flutter run
```

Never use the sample IDs in a production app. Follow the package README and
replace both platform App IDs and every configured ad unit ID.
